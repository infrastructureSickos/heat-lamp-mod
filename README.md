# Heat Lamp

A custom Forge 1.20.1 mod for the InfrastructureSickos Minecraft server.

## Building

```bash
./gradlew build
```

The built jar will be in `build/libs/`.

## Publishing

Releases are published automatically to Modrinth via GitHub Actions when a `v*` tag is pushed.
